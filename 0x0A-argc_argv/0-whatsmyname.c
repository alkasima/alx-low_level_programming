#include "main.h"
#include <stdio.h>

/**
 * main - Prints the name of the program
 * @argc: Number of arguments
 * @argv: Array name
 * Return: 0 success
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
