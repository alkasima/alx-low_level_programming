#include "main.h"

/**
 * mul - Multiplies the two integers
 * @x: The first integer to multiply
 * @y: The second number to multiply with the first int
 *
 * Return: The result of mul x param and y param
 */

int mul(int x, int y)
{
	return (x * y);
}
