#include "main.h"

/**
 * main -test the positive_or_negative function to return if number is negative or positive
 * Return: 0 always success
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
