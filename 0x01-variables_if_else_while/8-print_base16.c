#include <stdio.h>

/**
 * main - Starting point
 *
 * Return: 0 always success
 */

int main(void)
{
	char ch;

	for (ch = '0'; c <= '9'; ch++)
	{
		putchar(c);
	}

	for (ch = 'a'; ch <= 'f'; ch++)
	{
		putchar(ch);
	}

	putchar('\n');

	return (0);
}
